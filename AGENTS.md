# 项目代理约定

本文件适用于墨校台仓库及全部子目录。

## 强制规则

1. 所有会话、计划、审计结论和项目文档使用中文。
2. 编程工作遵循 [CLAUDE.md](CLAUDE.md)：先理解事实源和现有实现，再做最小且可验证的改动。
3. 活母本、语义版本和不可变出版快照必须分离；界面不得绕过领域层直接改写持久化数据。
4. 用户工作区中的未提交改动均视为用户成果，不得回滚、覆盖或顺手整理无关内容。
5. 改变架构、数据契约、测试方式、发布边界或视觉令牌时，同步更新对应文档和 `CHANGELOG.md`。

## 验收底线

- TypeScript 改动至少运行 `pnpm typecheck` 与相关单元测试。
- 领域、交换和出版契约改动必须包含往返或能力预检测试。
- UI 改动至少验证桌面宽度、窄窗口、深色模式和减弱动态效果，并保留实际截图。
- 报告必须区分代码失败、数据失败与本机环境失败，不把未执行的测试写成通过。
