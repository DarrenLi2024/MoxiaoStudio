# 更新日志

## Unreleased - MVP2 智校流

- 新增版本化智校建议协议、本地离线一致性检查器、字段白名单和模型响应校验；AI 永不直接写入活母本。
- 新增建议收件箱，逐条显示原值、建议值、理由、证据和置信度；接受前复核母本基线，冲突时拒绝覆盖。
- 新增 SQLite 智校运行与人工决定审计，只保存输入哈希和任务摘要，不复制完整请求正文。
- 新增可选 OpenAI-compatible BYOK 连接器；密钥由 Electron 系统安全存储加密，远端调用必须由用户确认发送范围。
- 批量补录升级为“解析暂存—人工预览—确认入库”，版本中心可查看并恢复不可变语义版本；`⌘K` 搜索和主导航入口均具备真实行为。

## 0.4.0 - 2026-08-19

- 将出版项目升级为 1.3，新增 21 类版面元素的语义样式矩阵与确定性继承；1.0—1.2 项目自动迁移并保持旧版诗词 13pt、2 倍行距视觉。
- 修复“调正文字号却只改变译文、笺注与赏析”的根因：PDF 与 EPUB 统一消费语义样式表，诗词正文、散文正文、译文、笺注、赏析及各级标题可独立设置。
- 新增字体、字号、字重、行距、字距、对齐、缩进、段前段后、颜色、下划线、上下装饰线、边框、底色、圆角和内边距配置，以及继承来源提示。
- 新增属性级锁定、整组锁定与恢复继承；切换四套主题时仅更新未锁定项，已确认的局部排印效果保持不变。
- 新增以 GB/T 788-1999 为尺寸依据的 A4/A5/B5 开本元数据，以及上下/内外边距、对页镜像、单/双栏、栏间距和基线网格设置；PDF 输出映射镜像页与分栏，EPUB 对固定版能力给出可重排适配提示。
- 重构出版中心样式步骤为“主题预设—版面元素—属性检查器—实时成书预览”的版式工坊，并扩展深色、窄屏与减弱动态效果门禁。
- 增加旧项目迁移、元素独立性、主题锁定、PDF 类映射及真实 Electron 计算样式回归。
- macOS 发布改为显式应用/DMG 两阶段公证，避免 electron-builder 隐式子进程丢失钥匙串配置，并保证 DMG 内部应用已装订公证票据。

## 0.3.2 - 2026-08-18

- 修复出版预览 `srcDoc` 继承桌面端 CSP 后整段样式表被拒绝执行的问题；使用父子一致的受控 nonce，恢复字体、标题对齐、字号、行距、章饰、译文、笺注与赏析主题的真实渲染。
- 图片焦点从行内 `style` 改为受控样式类，保持严格 CSP，不以全局 `unsafe-inline` 换取表面可用。
- 将导出预检升级为集中修正工作台：同一素材在多篇或多处触发的问题按素材 ID 归并，可就地填写替代文字、确认使用权或定位到对应步骤。
- 新增基于图注、书名和关联篇目的替代文字草稿生成；明确保留人工复核，并禁止系统擅自推断素材权属。
- Electron 回归新增真实计算样式、样式表挂载、集中预检解除门禁，以及正式 SQLite 隔离副本上的重复素材归并测试。

## 0.3.1 - 2026-08-18

- 将出版项目升级为 1.2，旧单体裁筛选确定性迁移为多体裁集合；14 类实际体裁统一使用中文事实源，未知体裁代码仍安全保留。
- 重构编排工作台：筛选范围、界面清单、目录和导出共享同一顺序；动态排序明确区分于作者编定，并可一键固化当前可见顺序。
- 样式预览改为本地同步渲染、后台异步持久化；新增段落节奏、章首留白、诗行对齐、版心宽度及书页/平板/手机预览。
- EPUB 升级为 3.3 兼容封装，补齐正文地标、阅读顺序、版权/出版社、可重排声明和基础无障碍发现元数据；响应式 CSS 尊重阅读器前景、背景与字号设置。
- 新增通用 EPUB、Apple Books 和中文阅读器兼容基线；Apple Books 提示封面短边 1400 像素，未公开平台规范不虚构为认证能力。
- Electron 端到端回归覆盖体裁多选、系年倒序、顺序固化、样式实时渲染及 PDF/EPUB 全链路；真实 EPUB 经 EPUBCheck 3.3 验证为零错误零警告。

## 0.3.0 - 2026-08-18

- 将出版项目升级为 1.1，并对 1.0 项目执行确定性迁移；新增版权页、书名页、前言、作者简介、目录、字数统计和确认状态。
- 校勘记、版本说明和审校说明默认不再逐篇输出，可显式汇总为书末附录或内部审校附录。
- 新增按体裁、系年、意境和综合策展的可解释编排候选，支持篇目锁定、应用与撤销，不改写母本编次。
- 新增作者照片和单篇多插图位置；支持题图、随文锚点、独立插页、尾花、焦点/尺寸/对齐设置及项目图库复用。
- 新增四套语义主题及标题、译文、笺注、赏析、字体、字号、行距、密度、主题色和章饰组合，PDF 与 EPUB 共用 Publication IR。
- 出版中心重构为六步工作流；正式出版事实、草稿确认、插图替代文字/权利和锚点失配进入明确预检。
- 真实 Electron 回归覆盖六步出版、意境编排、单篇插图、PDF/EPUB、深色窄屏、115% 缩放与减弱动态效果；新增正式 SQLite 在线备份的整卷隔离测试。

## 0.2.2 - 2026-08-17

- 修复整卷书稿较大时 PDF 导出使用超长 `data:` URL，触发 Chromium `ERR_INVALID_URL (-300)` 的问题。
- 隐藏打印窗口改为加载权限为 `0600` 的随机临时 HTML 文件，导出结束后立即清理，不再受 URL 长度上限约束。
- 安装包冷启动回归增加真实 PDF 导出及成品结构校验；另以 228 篇生产资料库隔离副本完成 745 页大书稿验收。

## 0.2.1 - 2026-08-17

- 修复生产工作区含旧版字符串校勘记时，点击“出版”因预览生成异常而无响应的问题。
- 旧校勘记在验证原基线摘要后稳定迁移为结构化记录，迁移后重算基线摘要，不掩盖损坏或篡改。
- 出版文档生成增加旧数据容错；出版中心打开失败时显示明确错误，不再误标为保存失败。
- 安装包冷启动回归增加真实点击出版、打开出版中心和通过预检的验收。

## 0.2.0 - 2026-08-17

- 修复窄屏首次启动时语义检查器浮层遮挡笺读操作区的问题，并增加响应式 E2E 门禁。
- macOS 封装复用锁定依赖中的 Electron 运行时，避免发行阶段重复下载导致构建挂起。
- 将笺读占位入口升级为完整编校界面，支持今译、正文锚定笺注、出处、赏析、校勘记、版本说明和审校说明，并纳入自动保存与旧审校包往返。
- 新增独立于活母本的多书稿出版项目，可按体裁、系年、作者编定顺序筛选、收录和排序，不修改作品原始编次。
- 新增自定义封面、篇章插图、替代文字和资产权利门禁；支持导入授权字体、字体选择、字号、行距、主题色和章饰。
- 新增可重排 EPUB 3 适配器，输出导航、元数据、章节、样式、图片和嵌入字体，并验证 ZIP/EPUB 必需条目。
- 新增 PDF、EPUB、闲心子墨暂存包和 WebPub 内容包的目标切换；各适配器声明真实能力，不支持的水印、页眉或字体要求会阻止导出。
- 扩展真实 Electron 回归，覆盖笺读重启持久化、PDF/EPUB 成品及窄屏深色减弱动态效果。
- 将 EPUBCheck 5.3.0 纳入发行门禁，修复空简介生成无效 EPUB 3.3 元数据的问题。
- 生成 Developer ID 签名、公证并装订的 macOS 0.2.0 Apple Silicon DMG，完成 Gatekeeper 与带隔离属性的包内冷启动验收。

## 0.1.2 - 2026-08-12

- 修复首次安装后内置演示文稿阻止全量审校备份导入的问题；仅允许完整备份替换内容未被修改的初始演示区，自动保存产生的修订号不影响判断，真实用户记录继续受冲突门禁保护。

## 0.1.1 - 2026-08-11

- 修复互联网下载后 Gatekeeper 报告“应用已损坏”的发行缺陷。
- 公开包强制使用 Developer ID Application 签名、Hardened Runtime 和安全时间戳。
- 建立应用与 DMG 双层公证、票据装订、Gatekeeper 验收和 SHA-256 清单链路。
- 修正 DMG 容器签名顺序，确保公证票据之外还具备可供 Gatekeeper 验证的 Developer ID 签名。
- 缺少签名或公证凭据时失败关闭，避免再次误发布临时签名包。

## 0.1.0 - 2026-08-11

- 建立独立墨校台 monorepo 与中文工程文档体系。
- 建立 Work、Expression、Manifestation、Item 四层领域语义。
- 建立 UUIDv7 文稿节点、墓碑和节点级三方合并契约。
- 建立 Ontology v1 与 Publication IR、渲染能力预检。
- 建立 Electron + React 桌面工作台地基与首版视觉系统。
- 接入 SQLite WAL、乐观修订锁、恢复日志、不可变语义版本、墓碑与 Outbox。
- 迁移 XZM-EW JSON 导入导出、体裁与状态筛选、新增、批量补录、备份后清空及双栏查重治理。
- 建立真实 Electron 端到端回归，覆盖编辑自动保存、重启持久化、筛选与查重。
- 建立出版中心，落实纸张、水印、页眉页脚、页码与 PDF 规范配置。
- 接入分页 HTML 与真实 Tagged PDF 导出，增加资源权利、乱码及 PDF 成品验证门禁。
- 建立闲心子墨内容适配器和 `XZM-XIANXIN-CONTENT 1.0` 内容包，覆盖作品、笺读、校音、朗读及媒体清单。
- 用真实闲心子墨工作树完成 233 条作品记录的无损语义往返，并保持源仓库只读。
- 开放 MPL-2.0 许可证，补齐隐私、内容权利、AIGC、第三方声明、社区规范与发布门禁。
- 升级 Electron 41.10.3，修复已公开高危漏洞，并增加 CSP、导航拦截与 IPC 输入边界。
- 建立 macOS Apple Silicon DMG 构建配置和 GitHub CI/Dependabot 基线。
- 生成并挂载验收首个 Apple Silicon DMG，从安装包内完成冷启动冒烟测试。
- 稳定云端窄屏下的出版主入口定位，并放宽 PDF 成品验收等待窗口。
- GitHub Actions 升级到 Node 24 运行时的官方主版本，消除旧运行时弃用隐患。
