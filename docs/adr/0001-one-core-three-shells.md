# ADR-0001：一套内核、三种运行形态

## 状态

已接受。

## 决策

墨校台以 Electron 桌面端作为主产品，以自托管 Web 作为后续协作形态，以 CLI 作为自动化入口。三者共享领域、Ontology 和出版契约，通过存储、身份、资源、任务与渲染端口适配不同环境。

## 后果

- 普通桌面用户无需理解本地端口。
- 现有 Web 工作台能力可以渐进迁移。
- Web 版不能直接照搬本地文件权限，必须使用服务端权限与对象存储。
- P0 不拆微服务，但禁止领域包依赖 Electron 或具体数据库。
