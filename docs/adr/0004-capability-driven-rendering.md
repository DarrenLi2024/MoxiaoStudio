# ADR-0004：能力驱动的出版渲染

## 状态

已接受。

## 决策

Publication IR 与渲染引擎解耦。每个渲染器必须声明版本化能力；预检逐项比较出版配置与能力，不支持时阻止或产生显式降级决策。

## 后果

- 可以同时接入开源预览和专业印刷引擎。
- 水印、页眉页脚、竖排、出血、PDF/X 等不会被静默忽略。
- 出版模板可以跨渲染器复用语义，但视觉结果仍需分别建立回归基线。
