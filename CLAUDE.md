# Karpathy 工程规范

## 核心原则

- 先看数据，再写代码。作品字符底本、结构节点、语义标注、媒体与出版配置属于不同事实层，不为界面方便混写。
- 保持实现直接。只有两个以上真实使用点才提取抽象；扩展性通过稳定端口和版本化契约获得，不通过空壳框架获得。
- 不做英雄式重写。每批只交付可以独立运行、验证和回滚的纵向切片。
- Demo 不是完成。空项目、损坏数据、重复 ID、保存失败、缺字、无授权资源和渲染器能力不足都必须有明确结果。
- 让证据决定结论。类型检查、单元测试、构建产物、往返不变性和视觉截图优先于主观判断。

## 项目不变量

- `Work` 表示抽象作品，`Expression` 表示可继续演进的语义表达，`Manifestation` 表示不可变出版快照，`Item` 表示实际文件。
- 字符内容不注入拼音、批注、时间轴或编辑标记；这些信息以稳定节点 ID 锚定。
- 节点 ID 使用 UUIDv7，删除生成墓碑，ID 永不复用。
- 并发变更采用以节点 ID 为键的 base/ours/theirs 三方合并；同节点双向修改必须显式冲突。
- 所有渲染器声明能力清单；出版配置请求不受支持的能力时必须阻止导出或获得用户明确降级授权。
- 本地离线完成核心编校和出版；联网 AI 只能提供建议，不能绕过人工确认和审计记录。

## 标准流程

1. 阅读相关文档、领域模型和相似实现。
2. 写出可独立验收的步骤。
3. 先实现事实规则和错误边界，再实现界面。
4. 运行最窄测试，再运行 `pnpm check`。
5. 检查 `git diff`，避免秘密、用户内容、缓存和未授权资产进入仓库。
